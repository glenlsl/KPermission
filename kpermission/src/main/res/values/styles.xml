<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- ==================权限申请对话框（alertdialog）================== -->
    <style name="style_permission_dialog" parent="AlertDialog.AppCompat.Light">
        <!--<style name="style_permission_dialog" parent="Theme.AppCompat.Light.Dialog.Alert">-->
        <!-- Dialog的windowFrame框为无 -->
        <item name="android:windowFrame">@null</item>
        <!-- 是否显示title -->
        <item name="android:windowNoTitle">false</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 设置dialog的背景：#00000000透明色 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景变灰：整个屏幕变灰，配合setCanceledOnTouchOutside(false) -->
        <item name="android:backgroundDimEnabled">true</item>
    </style>


    <style name="AnimActivity" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowAnimationStyle">@style/AnimationActivity</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="AnimationActivity" parent="@android:style/Animation.Activity">
        <item name="android:activityOpenEnterAnimation">@anim/act_fade_in</item>
        <item name="android:activityOpenExitAnimation">@anim/act_fade_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/act_fade_in</item>
        <item name="android:activityCloseExitAnimation">@anim/act_fade_out</item>
    </style>
</resources>